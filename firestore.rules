rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ MVP MODE - OPEN ACCESS FOR TESTING
    // TODO: Restore proper security rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}